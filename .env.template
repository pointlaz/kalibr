# Recording parameters (will be mounted as /recordings in the container)
HOST_RECORDING_PATH="$HOME/Data"

# Calibration parameters
CALIBRATION_DATE="YYYY-MM-DD"
BAGS_BASE_PATH="/recordings/calibrations/${CALIBRATION_DATE}/bags"

# Kalibr parameters
CAMERA_MODEL=pinhole
DISTORTION_MODEL=radtan
MUTUAL_INFORMATION_TOLERANCE=0.1
